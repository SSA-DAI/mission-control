# PLATFORM-004c — Deploy Guardrail Live Probe

**Status:** PROBE — safe to delete. This branch intentionally uses the `deploy/`
prefix to verify the PLATFORM-004c guardrail: branches matching `deploy/*` must be
pushed but must **never** get an auto-PR.

Task: `2a7ff09e-4615-43d1-918e-2017b2c4c18d` (Add PLATFORM-004c deploy guardrail probe)
Created: 2026-08-06 ~01:32 WIB

Expected outcome after auto-merge:
- [x] Branch pushed to origin (remote stays in sync)
- [x] `merge_status = merged` (NOT pr_created)
- [x] `merge_pr_url = null` (no PR created)
- [x] Zero new PRs on the repo from this branch

Delete this branch after verification.
